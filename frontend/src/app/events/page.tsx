import Events from './components/Events';

export const metadata = {
    title: "Events",
    description: "Stay connected with our community through workshops, meetings, and social events.",
};


export default function EventsPage() {
    return <Events />;
}